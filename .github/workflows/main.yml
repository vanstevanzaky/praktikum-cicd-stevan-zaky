name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  test-logic:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Dependencies
        run: npm install

      - name: Run Unit Tests
        run: npm test

  build-image:
    runs-on: ubuntu-latest
    needs: test-logic
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build Docker Image
        run: docker build -t praktikum-cicd-stevan-zaky .
